name: statyx
description: State management with MVVM pattern
version: 0.0.6
homepage: https://github.com/elliotkhd/StatyX

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
